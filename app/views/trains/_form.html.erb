<%= form_for @train do |f| %>

    <%= render 'common/errors', resource: @train %>

    <p>
      <%= f.label :title %><br>
      <%= f.text_field :tnumber %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>

<%= link_to 'Back', trains_path %>